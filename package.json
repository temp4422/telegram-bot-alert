{
  "name": "telegram-bot-alert",
  "version": "3.0.0",
  "description": "telegram bot that uses puppeteer to scrape data from website and send alert to telegram",
  "type": "module",
  "main": "./dist/app.js",
  "author": "temp4422",
  "license": "ISC",
  "keywords": [
    "telegram",
    "bot"
  ],
  "engines": {
    "node": "^24",
    "npm": "^11"
  },
  "scripts": {
    "dev": "dotenvx run -f .env.production -- node --watch ./src/app.js",
    "dev:docker": "docker compose -f docker-compose.dev.yaml up --build",
    "prebuild": "rm -rf ./dist/ && mkdir ./dist/",
    "build": "node ./build.js",
    "start": "dotenvx run -f .env.production -- node ./dist/app.js",
    "start:docker": "docker compose up -d --build"
  },
  "dependencies": {
    "@dotenvx/dotenvx": "^2.21.0",
    "puppeteer": "^25.8.0"
  }
}
